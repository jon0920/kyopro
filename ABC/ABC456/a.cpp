#include <bits/stdc++.h>
using namespace std;
using ll = long long;
#define rep(i,n) for(int i = 0; i < (n); i++)

int main(){

    int x;
    cin >> x;

    if(x >= 3 && x <= 18) cout << "Yes" << endl;
    else cout << "No" << endl;

    return 0;
}