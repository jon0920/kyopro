#include <bits/stdc++.h>
#include <cmath>
using namespace std;
#define rep(i,n) for(int i = 0; i < n; i++)
using ll = long long;

int main() {
    
    double a, b;
    cin >> a >> b;
    double c = a / b;

    cout << round(c) << endl;
    
    return 0;
}