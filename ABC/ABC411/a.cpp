#include <bits/stdc++.h>
using namespace std;
#define rep(i,n) for(int i = 0; i < n; i++)
using ll = long long;

int main() {
    
    string p;
    int l;
    cin >> p >> l;
    cout << (p.size() >= l ? "Yes" : "No") << endl;
    
    return 0;
}