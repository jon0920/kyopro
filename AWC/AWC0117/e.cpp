#include <bits/stdc++.h>
#include <atcoder/modint>
using namespace atcoder;
using namespace std;
using ll = long long;
#define rep(i,n) for(int i = 0; i < (n); i++)
using mint = modint;

int main(){
    
    int k, n, m;
    cin >> k >> n >> m;
    mint::set_mod(m);

    vector<int> a(n);
    rep(i,n) cin >> a[i];
    

    
    return 0;
}